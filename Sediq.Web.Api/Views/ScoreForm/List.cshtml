@model List<Example.Service.Models.DTOs.ScoreFormResponseDto>
@{
    ViewData["Title"] = "لیست نمرات";
}

<div class="container-fluid mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2>لیست نمرات</h2>
        <a asp-action="Create" class="btn btn-primary">افزودن نمره جدید</a>
    </div>

    @if (TempData["Success"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show">
            @TempData["Success"]
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    }

    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead class="table-dark">
                <tr>
                    <th>ردیف</th>
                    <th>نام برنامه</th>
                    <th>نام شرکت کننده</th>
                    <th>نمره</th>
                    <th>بازه زمانی</th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Any())
                {
                    int counter = 1;
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@counter</td>
                            <td>@item.ProgramName</td>
                            <td>@item.StudentName</td>
                            <td>@item.Score</td>
                            <td>@item.DurationName</td>
                            <td>
                                <button class="btn btn-sm btn-info" onclick="showDetails(@item.RowId)">جزئیات</button>
                                <button class="btn btn-sm btn-warning" onclick="showEdit(@item.RowId)">ویرایش</button>
                                <button class="btn btn-sm btn-danger" onclick="showDelete(@item.RowId)">حذف</button>
                            </td>
                        </tr>
                        counter++;
                    }
                }
                else
                {
                    <tr>
                        <td colspan="6" class="text-center">نمره‌ای یافت نشد</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<div class="modal fade" id="detailsModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">جزئیات نمره</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="detailsContent"></div>
        </div>
    </div>
</div>

<div class="modal fade" id="editModal" tabindex="-1">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">ویرایش نمره</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="editContent"></div>
        </div>
    </div>
</div>

<div class="modal fade" id="deleteModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">حذف نمره</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <p>آیا از حذف این نمره اطمینان دارید؟</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">انصراف</button>
                <button type="button" class="btn btn-danger" id="confirmDelete">حذف</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        let currentId = 0;
        function showDetails(id) {
            $.get('@Url.Action("Details", "ScoreForm")/' + id, function(data) {
                $('#detailsContent').html(data);
                $('#detailsModal').modal('show');
            });
        }
        function showEdit(id) {
            $.get('@Url.Action("Edit", "ScoreForm")/' + id, function(data) {
                $('#editContent').html(data);
                $('#editModal').modal('show');
            });
        }
        function showDelete(id) {
            currentId = id;
            $('#deleteModal').modal('show');
        }
        $('#confirmDelete').click(function() {
            $.post('@Url.Action("Delete", "ScoreForm")/' + currentId, function(response) {
                if (response.success) {
                    location.reload();
                } else {
                    alert(response.message);
                }
            });
        });
        $(document).on('submit', '#editForm', function(e) {
            e.preventDefault();
            $.post($(this).attr('action'), $(this).serialize(), function(response) {
                if (response.success) {
                    location.reload();
                } else {
                    alert(response.message);
                }
            });
        });
    </script>
}
